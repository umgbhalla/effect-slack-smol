/**
 * Generated Slack Openid Service
 * DO NOT EDIT - This file is auto-generated by scripts/generate-services.ts
 */

import { Context, Effect, Layer } from "effect"
import type {
  OpenIDConnectTokenArguments,
  OpenIDConnectTokenResponse,
  OpenIDConnectUserInfoArguments,
  OpenIDConnectUserInfoResponse
} from "@slack/web-api"
import { SlackConfig } from "../SlackConfig.js"
import { SlackClient } from "../SlackClient.js"
import { mapSlackError, annotateSpanWithError, type SlackError } from "../internal/errors.js"

export class OpenidService extends Context.Service<OpenidService>()("effect-slack/OpenidService", {
  make: Effect.gen(function* () {
    const client = yield* SlackClient

    /**
     * Exchanges a temporary OAuth verifier code for an access token for {@link https://docs.slack.dev/authentication/sign-in-with-slack Sign in with Slack}.
     */
    const ConnectToken = Effect.fn("OpenidService.connect.token", {
      attributes: { "slack.method": "openid.connect.token" }
    })(
      (args: OpenIDConnectTokenArguments): Effect.Effect<OpenIDConnectTokenResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.openid.connect.token(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Get the identity of a user who has authorized {@link https://docs.slack.dev/authentication/sign-in-with-slack Sign in with Slack}.
     */
    const ConnectUserInfo = Effect.fn("OpenidService.connect.userInfo", {
      attributes: { "slack.method": "openid.connect.userInfo" }
    })(
      (args?: OpenIDConnectUserInfoArguments): Effect.Effect<OpenIDConnectUserInfoResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.openid.connect.userInfo(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    return {
      connect: {
        token: ConnectToken,
        userInfo: ConnectUserInfo
      }
    } as const
  })
}) {
  static readonly layer: Layer.Layer<OpenidService, never, SlackClient> = Layer.effect(
    OpenidService,
    OpenidService.make
  )

  static readonly Default: Layer.Layer<OpenidService, never, SlackConfig> = OpenidService.layer.pipe(
    Layer.provide(SlackClient.Default)
  )
}
