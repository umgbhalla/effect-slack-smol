/**
 * Generated Slack Oauth Service
 * DO NOT EDIT - This file is auto-generated by scripts/generate-services.ts
 */

import { Context, Effect, Layer } from "effect"
import type {
  OAuthAccessArguments,
  OAuthV2AccessArguments,
  OAuthV2ExchangeArguments,
  OauthAccessResponse,
  OauthV2AccessResponse,
  OauthV2ExchangeResponse
} from "@slack/web-api"
import { SlackConfig } from "../SlackConfig.js"
import { SlackClient } from "../SlackClient.js"
import { mapSlackError, annotateSpanWithError, type SlackError } from "../internal/errors.js"

export class OauthService extends Context.Service<OauthService>()("effect-slack/OauthService", {
  make: Effect.gen(function* () {
    const client = yield* SlackClient

    /**
     * Exchanges a temporary OAuth verifier code for an access token.
     * @deprecated
     */
    const access = Effect.fn("OauthService.access", {
      attributes: { "slack.method": "oauth.access" }
    })(
      (args: OAuthAccessArguments): Effect.Effect<OauthAccessResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.oauth.access(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Exchanges a temporary OAuth verifier code for an access token.
     */
    const V2Access = Effect.fn("OauthService.v2.access", {
      attributes: { "slack.method": "oauth.v2.access" }
    })(
      (args: OAuthV2AccessArguments): Effect.Effect<OauthV2AccessResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.oauth.v2.access(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Exchanges a legacy access token for a new expiring access token and refresh token.
     */
    const V2Exchange = Effect.fn("OauthService.v2.exchange", {
      attributes: { "slack.method": "oauth.v2.exchange" }
    })(
      (args: OAuthV2ExchangeArguments): Effect.Effect<OauthV2ExchangeResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.oauth.v2.exchange(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    return {
      access,
      v2: {
        access: V2Access,
        exchange: V2Exchange
      }
    } as const
  })
}) {
  static readonly layer: Layer.Layer<OauthService, never, SlackClient> = Layer.effect(
    OauthService,
    OauthService.make
  )

  static readonly Default: Layer.Layer<OauthService, never, SlackConfig> = OauthService.layer.pipe(
    Layer.provide(SlackClient.Default)
  )
}
