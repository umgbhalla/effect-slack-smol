/**
 * Generated Slack Workflows Service
 * DO NOT EDIT - This file is auto-generated by scripts/generate-services.ts
 */

import { Context, Effect, Layer } from "effect"
import type {
  WebAPICallResult,
  WorkflowsFeaturedAddArguments,
  WorkflowsFeaturedListArguments,
  WorkflowsFeaturedListResponse,
  WorkflowsFeaturedRemoveArguments,
  WorkflowsFeaturedSetArguments,
  WorkflowsStepCompletedArguments,
  WorkflowsStepCompletedResponse,
  WorkflowsStepFailedArguments,
  WorkflowsStepFailedResponse,
  WorkflowsUpdateStepArguments,
  WorkflowsUpdateStepResponse
} from "@slack/web-api"
import { SlackConfig } from "../SlackConfig.js"
import { SlackClient } from "../SlackClient.js"
import { mapSlackError, annotateSpanWithError, type SlackError } from "../internal/errors.js"

export class WorkflowsService extends Context.Service<WorkflowsService>()("effect-slack/WorkflowsService", {
  make: Effect.gen(function* () {
    const client = yield* SlackClient

    /**
     * Indicate that an app's step in a workflow completed execution.
     * @deprecated
     */
    const stepCompleted = Effect.fn("WorkflowsService.stepCompleted", {
      attributes: { "slack.method": "workflows.stepCompleted" }
    })(
      (args: WorkflowsStepCompletedArguments): Effect.Effect<WorkflowsStepCompletedResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.workflows.stepCompleted(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Indicate that an app's step in a workflow failed to execute.
     * @deprecated
     */
    const stepFailed = Effect.fn("WorkflowsService.stepFailed", {
      attributes: { "slack.method": "workflows.stepFailed" }
    })(
      (args: WorkflowsStepFailedArguments): Effect.Effect<WorkflowsStepFailedResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.workflows.stepFailed(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Update the configuration for a workflow step.
     * @deprecated
     */
    const updateStep = Effect.fn("WorkflowsService.updateStep", {
      attributes: { "slack.method": "workflows.updateStep" }
    })(
      (args: WorkflowsUpdateStepArguments): Effect.Effect<WorkflowsUpdateStepResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.workflows.updateStep(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Add featured workflows to a channel.
     */
    const FeaturedAdd = Effect.fn("WorkflowsService.featured.add", {
      attributes: { "slack.method": "workflows.featured.add" }
    })(
      (args: WorkflowsFeaturedAddArguments): Effect.Effect<WebAPICallResult, SlackError> =>
        Effect.tryPromise({
          try: () => client.workflows.featured.add(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * List the featured workflows for specified channels.
     */
    const FeaturedList = Effect.fn("WorkflowsService.featured.list", {
      attributes: { "slack.method": "workflows.featured.list" }
    })(
      (args: WorkflowsFeaturedListArguments): Effect.Effect<WorkflowsFeaturedListResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.workflows.featured.list(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Remove featured workflows from a channel.
     */
    const FeaturedRemove = Effect.fn("WorkflowsService.featured.remove", {
      attributes: { "slack.method": "workflows.featured.remove" }
    })(
      (args: WorkflowsFeaturedRemoveArguments): Effect.Effect<WebAPICallResult, SlackError> =>
        Effect.tryPromise({
          try: () => client.workflows.featured.remove(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Set featured workflows for a channel.
     */
    const FeaturedSet = Effect.fn("WorkflowsService.featured.set", {
      attributes: { "slack.method": "workflows.featured.set" }
    })(
      (args: WorkflowsFeaturedSetArguments): Effect.Effect<WebAPICallResult, SlackError> =>
        Effect.tryPromise({
          try: () => client.workflows.featured.set(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    return {
      stepCompleted,
      stepFailed,
      updateStep,
      featured: {
        add: FeaturedAdd,
        list: FeaturedList,
        remove: FeaturedRemove,
        set: FeaturedSet
      }
    } as const
  })
}) {
  static readonly layer: Layer.Layer<WorkflowsService, never, SlackClient> = Layer.effect(
    WorkflowsService,
    WorkflowsService.make
  )

  static readonly Default: Layer.Layer<WorkflowsService, never, SlackConfig> = WorkflowsService.layer.pipe(
    Layer.provide(SlackClient.Default)
  )
}
