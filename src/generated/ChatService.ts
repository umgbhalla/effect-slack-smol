/**
 * Generated Slack Chat Service
 * DO NOT EDIT - This file is auto-generated by scripts/generate-services.ts
 */

import { Context, Effect, Layer } from "effect"
import type {
  ChatAppendStreamArguments,
  ChatAppendStreamResponse,
  ChatDeleteArguments,
  ChatDeleteResponse,
  ChatDeleteScheduledMessageArguments,
  ChatDeleteScheduledMessageResponse,
  ChatGetPermalinkArguments,
  ChatGetPermalinkResponse,
  ChatMeMessageArguments,
  ChatMeMessageResponse,
  ChatPostEphemeralArguments,
  ChatPostEphemeralResponse,
  ChatPostMessageArguments,
  ChatPostMessageResponse,
  ChatScheduleMessageArguments,
  ChatScheduleMessageResponse,
  ChatScheduledMessagesListArguments,
  ChatScheduledMessagesListResponse,
  ChatStartStreamArguments,
  ChatStartStreamResponse,
  ChatStopStreamArguments,
  ChatStopStreamResponse,
  ChatUnfurlArguments,
  ChatUnfurlResponse,
  ChatUpdateArguments,
  ChatUpdateResponse
} from "@slack/web-api"
import { SlackConfig } from "../SlackConfig.js"
import { SlackClient } from "../SlackClient.js"
import { mapSlackError, annotateSpanWithError, type SlackError } from "../internal/errors.js"

export class ChatService extends Context.Service<ChatService>()("effect-slack/ChatService", {
  make: Effect.gen(function* () {
    const client = yield* SlackClient

    /**
     * Appends text to an existing streaming conversation.
     */
    const appendStream = Effect.fn("ChatService.appendStream", {
      attributes: { "slack.method": "chat.appendStream" }
    })(
      (args: ChatAppendStreamArguments): Effect.Effect<ChatAppendStreamResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.chat.appendStream(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Deletes a message.
     */
    const delete_ = Effect.fn("ChatService.delete_", {
      attributes: { "slack.method": "chat.delete" }
    })(
      (args: ChatDeleteArguments): Effect.Effect<ChatDeleteResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.chat.delete(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Deletes a pending scheduled message from the queue.
     */
    const deleteScheduledMessage = Effect.fn("ChatService.deleteScheduledMessage", {
      attributes: { "slack.method": "chat.deleteScheduledMessage" }
    })(
      (args: ChatDeleteScheduledMessageArguments): Effect.Effect<ChatDeleteScheduledMessageResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.chat.deleteScheduledMessage(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Retrieve a permalink URL for a specific extant message.
     */
    const getPermalink = Effect.fn("ChatService.getPermalink", {
      attributes: { "slack.method": "chat.getPermalink" }
    })(
      (args: ChatGetPermalinkArguments): Effect.Effect<ChatGetPermalinkResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.chat.getPermalink(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Share a me message into a channel.
     */
    const meMessage = Effect.fn("ChatService.meMessage", {
      attributes: { "slack.method": "chat.meMessage" }
    })(
      (args: ChatMeMessageArguments): Effect.Effect<ChatMeMessageResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.chat.meMessage(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Sends an ephemeral message to a user in a channel.
     */
    const postEphemeral = Effect.fn("ChatService.postEphemeral", {
      attributes: { "slack.method": "chat.postEphemeral" }
    })(
      (args: ChatPostEphemeralArguments): Effect.Effect<ChatPostEphemeralResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.chat.postEphemeral(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Sends a message to a channel.
     */
    const postMessage = Effect.fn("ChatService.postMessage", {
      attributes: { "slack.method": "chat.postMessage" }
    })(
      (args: ChatPostMessageArguments): Effect.Effect<ChatPostMessageResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.chat.postMessage(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Schedules a message to be sent to a channel.
     */
    const scheduleMessage = Effect.fn("ChatService.scheduleMessage", {
      attributes: { "slack.method": "chat.scheduleMessage" }
    })(
      (args: ChatScheduleMessageArguments): Effect.Effect<ChatScheduleMessageResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.chat.scheduleMessage(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Starts a new streaming conversation.
     */
    const startStream = Effect.fn("ChatService.startStream", {
      attributes: { "slack.method": "chat.startStream" }
    })(
      (args: ChatStartStreamArguments): Effect.Effect<ChatStartStreamResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.chat.startStream(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Stops a streaming conversation.
     */
    const stopStream = Effect.fn("ChatService.stopStream", {
      attributes: { "slack.method": "chat.stopStream" }
    })(
      (args: ChatStopStreamArguments): Effect.Effect<ChatStopStreamResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.chat.stopStream(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Provide custom unfurl behavior for user-posted URLs.
     */
    const unfurl = Effect.fn("ChatService.unfurl", {
      attributes: { "slack.method": "chat.unfurl" }
    })(
      (args: ChatUnfurlArguments): Effect.Effect<ChatUnfurlResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.chat.unfurl(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Updates a message.
     */
    const update = Effect.fn("ChatService.update", {
      attributes: { "slack.method": "chat.update" }
    })(
      (args: ChatUpdateArguments): Effect.Effect<ChatUpdateResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.chat.update(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Returns a list of scheduled messages.
     */
    const ScheduledMessagesList = Effect.fn("ChatService.scheduledMessages.list", {
      attributes: { "slack.method": "chat.scheduledMessages.list" }
    })(
      (args?: ChatScheduledMessagesListArguments): Effect.Effect<ChatScheduledMessagesListResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.chat.scheduledMessages.list(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    return {
      appendStream,
      delete_,
      deleteScheduledMessage,
      getPermalink,
      meMessage,
      postEphemeral,
      postMessage,
      scheduleMessage,
      startStream,
      stopStream,
      unfurl,
      update,
      scheduledMessages: {
        list: ScheduledMessagesList
      }
    } as const
  })
}) {
  static readonly layer: Layer.Layer<ChatService, never, SlackClient> = Layer.effect(
    ChatService,
    ChatService.make
  )

  static readonly Default: Layer.Layer<ChatService, never, SlackConfig> = ChatService.layer.pipe(
    Layer.provide(SlackClient.Default)
  )
}
