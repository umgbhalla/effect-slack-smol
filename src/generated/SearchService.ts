/**
 * Generated Slack Search Service
 * DO NOT EDIT - This file is auto-generated by scripts/generate-services.ts
 */

import { Context, Effect, Layer } from "effect"
import type {
  SearchAllArguments,
  SearchAllResponse,
  SearchFilesArguments,
  SearchFilesResponse,
  SearchMessagesArguments,
  SearchMessagesResponse
} from "@slack/web-api"
import { SlackConfig } from "../SlackConfig.js"
import { SlackClient } from "../SlackClient.js"
import { mapSlackError, annotateSpanWithError, type SlackError } from "../internal/errors.js"

export class SearchService extends Context.Service<SearchService>()("effect-slack/SearchService", {
  make: Effect.gen(function* () {
    const client = yield* SlackClient

    /**
     * Searches for messages and files matching a query.
     */
    const all = Effect.fn("SearchService.all", {
      attributes: { "slack.method": "search.all" }
    })(
      (args: SearchAllArguments): Effect.Effect<SearchAllResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.search.all(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Searches for files matching a query.
     */
    const files = Effect.fn("SearchService.files", {
      attributes: { "slack.method": "search.files" }
    })(
      (args: SearchFilesArguments): Effect.Effect<SearchFilesResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.search.files(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Searches for messages matching a query.
     */
    const messages = Effect.fn("SearchService.messages", {
      attributes: { "slack.method": "search.messages" }
    })(
      (args: SearchMessagesArguments): Effect.Effect<SearchMessagesResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.search.messages(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    return {
      all,
      files,
      messages
    } as const
  })
}) {
  static readonly layer: Layer.Layer<SearchService, never, SlackClient> = Layer.effect(
    SearchService,
    SearchService.make
  )

  static readonly Default: Layer.Layer<SearchService, never, SlackConfig> = SearchService.layer.pipe(
    Layer.provide(SlackClient.Default)
  )
}
