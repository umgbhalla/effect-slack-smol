/**
 * Generated Slack Apps Service
 * DO NOT EDIT - This file is auto-generated by scripts/generate-services.ts
 */

import { Context, Effect, Layer } from "effect"
import type {
  AppsConnectionsOpenArguments,
  AppsConnectionsOpenResponse,
  AppsEventAuthorizationsListArguments,
  AppsEventAuthorizationsListResponse,
  AppsManifestCreateArguments,
  AppsManifestCreateResponse,
  AppsManifestDeleteArguments,
  AppsManifestDeleteResponse,
  AppsManifestExportArguments,
  AppsManifestExportResponse,
  AppsManifestUpdateArguments,
  AppsManifestUpdateResponse,
  AppsManifestValidateArguments,
  AppsManifestValidateResponse,
  AppsUninstallArguments,
  AppsUninstallResponse,
  AppsUserConnectionUpdateArguments,
  AppsUserConnectionUpdateResponse
} from "@slack/web-api"
import { SlackConfig } from "../SlackConfig.js"
import { SlackClient } from "../SlackClient.js"
import { mapSlackError, annotateSpanWithError, type SlackError } from "../internal/errors.js"

export class AppsService extends Context.Service<AppsService>()("effect-slack/AppsService", {
  make: Effect.gen(function* () {
    const client = yield* SlackClient

    /**
     * Uninstalls your app from a workspace.
     */
    const uninstall = Effect.fn("AppsService.uninstall", {
      attributes: { "slack.method": "apps.uninstall" }
    })(
      (args: AppsUninstallArguments): Effect.Effect<AppsUninstallResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.apps.uninstall(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Generate a temporary Socket Mode WebSocket URL that your app can connect to in order to receive events and interactive payloads over.
     */
    const ConnectionsOpen = Effect.fn("AppsService.connections.open", {
      attributes: { "slack.method": "apps.connections.open" }
    })(
      (args?: AppsConnectionsOpenArguments): Effect.Effect<AppsConnectionsOpenResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.apps.connections.open(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Get a list of authorizations for the given event context. Each authorization represents an app installation that the event is visible to.
     */
    const EventAuthorizationsList = Effect.fn("AppsService.event.authorizations.list", {
      attributes: { "slack.method": "apps.event.authorizations.list" }
    })(
      (args: AppsEventAuthorizationsListArguments): Effect.Effect<AppsEventAuthorizationsListResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.apps.event.authorizations.list(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Create an app from an app manifest.
     */
    const ManifestCreate = Effect.fn("AppsService.manifest.create", {
      attributes: { "slack.method": "apps.manifest.create" }
    })(
      (args: AppsManifestCreateArguments): Effect.Effect<AppsManifestCreateResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.apps.manifest.create(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Permanently deletes an app created through app manifests.
     */
    const ManifestDelete_ = Effect.fn("AppsService.manifest.delete_", {
      attributes: { "slack.method": "apps.manifest.delete" }
    })(
      (args: AppsManifestDeleteArguments): Effect.Effect<AppsManifestDeleteResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.apps.manifest.delete(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Export an app manifest from an existing app.
     */
    const ManifestExport_ = Effect.fn("AppsService.manifest.export_", {
      attributes: { "slack.method": "apps.manifest.export" }
    })(
      (args: AppsManifestExportArguments): Effect.Effect<AppsManifestExportResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.apps.manifest.export(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Update an app from an app manifest.
     */
    const ManifestUpdate = Effect.fn("AppsService.manifest.update", {
      attributes: { "slack.method": "apps.manifest.update" }
    })(
      (args: AppsManifestUpdateArguments): Effect.Effect<AppsManifestUpdateResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.apps.manifest.update(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Validate an app manifest.
     */
    const ManifestValidate = Effect.fn("AppsService.manifest.validate", {
      attributes: { "slack.method": "apps.manifest.validate" }
    })(
      (args: AppsManifestValidateArguments): Effect.Effect<AppsManifestValidateResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.apps.manifest.validate(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Updates the connection status between a user and an app.
     */
    const UserConnectionUpdate = Effect.fn("AppsService.user.connection.update", {
      attributes: { "slack.method": "apps.user.connection.update" }
    })(
      (args: AppsUserConnectionUpdateArguments): Effect.Effect<AppsUserConnectionUpdateResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.apps.user.connection.update(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    return {
      uninstall,
      connections: {
        open: ConnectionsOpen
      },
      event: {
        authorizations: {
          list: EventAuthorizationsList
        }
      },
      manifest: {
        create: ManifestCreate,
        delete_: ManifestDelete_,
        export_: ManifestExport_,
        update: ManifestUpdate,
        validate: ManifestValidate
      },
      user: {
        connection: {
          update: UserConnectionUpdate
        }
      }
    } as const
  })
}) {
  static readonly layer: Layer.Layer<AppsService, never, SlackClient> = Layer.effect(
    AppsService,
    AppsService.make
  )

  static readonly Default: Layer.Layer<AppsService, never, SlackConfig> = AppsService.layer.pipe(
    Layer.provide(SlackClient.Default)
  )
}
