/**
 * Generated Slack Files Service
 * DO NOT EDIT - This file is auto-generated by scripts/generate-services.ts
 */

import { Context, Effect, Layer } from "effect"
import type {
  FilesCommentsDeleteArguments,
  FilesCommentsDeleteResponse,
  FilesCompleteUploadExternalArguments,
  FilesCompleteUploadExternalResponse,
  FilesDeleteArguments,
  FilesDeleteResponse,
  FilesGetUploadURLExternalArguments,
  FilesGetUploadURLExternalResponse,
  FilesInfoArguments,
  FilesInfoResponse,
  FilesListArguments,
  FilesListResponse,
  FilesRemoteAddArguments,
  FilesRemoteAddResponse,
  FilesRemoteInfoArguments,
  FilesRemoteInfoResponse,
  FilesRemoteListArguments,
  FilesRemoteListResponse,
  FilesRemoteRemoveArguments,
  FilesRemoteRemoveResponse,
  FilesRemoteShareArguments,
  FilesRemoteShareResponse,
  FilesRemoteUpdateArguments,
  FilesRemoteUpdateResponse,
  FilesRevokePublicURLArguments,
  FilesRevokePublicURLResponse,
  FilesSharedPublicURLArguments,
  FilesSharedPublicURLResponse,
  FilesUploadArguments,
  FilesUploadResponse,
  FilesUploadV2Arguments,
  WebAPICallResult
} from "@slack/web-api"
import { SlackConfig } from "../SlackConfig.js"
import { SlackClient } from "../SlackClient.js"
import { mapSlackError, annotateSpanWithError, type SlackError } from "../internal/errors.js"

export class FilesService extends Context.Service<FilesService>()("effect-slack/FilesService", {
  make: Effect.gen(function* () {
    const client = yield* SlackClient

    /**
     * Finishes an upload started with {@link https://docs.slack.dev/reference/methods/files.getUploadURLExternal `files.getUploadURLExternal`}.
     */
    const completeUploadExternal = Effect.fn("FilesService.completeUploadExternal", {
      attributes: { "slack.method": "files.completeUploadExternal" }
    })(
      (args: FilesCompleteUploadExternalArguments): Effect.Effect<FilesCompleteUploadExternalResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.files.completeUploadExternal(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Deletes a file.
     */
    const delete_ = Effect.fn("FilesService.delete_", {
      attributes: { "slack.method": "files.delete" }
    })(
      (args: FilesDeleteArguments): Effect.Effect<FilesDeleteResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.files.delete(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Gets a URL for an edge external file upload.
     */
    const getUploadURLExternal = Effect.fn("FilesService.getUploadURLExternal", {
      attributes: { "slack.method": "files.getUploadURLExternal" }
    })(
      (args: FilesGetUploadURLExternalArguments): Effect.Effect<FilesGetUploadURLExternalResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.files.getUploadURLExternal(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Gets information about a file.
     */
    const info = Effect.fn("FilesService.info", {
      attributes: { "slack.method": "files.info" }
    })(
      (args: FilesInfoArguments): Effect.Effect<FilesInfoResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.files.info(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * List files for a team, in a channel, or from a user with applied filters.
     */
    const list = Effect.fn("FilesService.list", {
      attributes: { "slack.method": "files.list" }
    })(
      (args: FilesListArguments): Effect.Effect<FilesListResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.files.list(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Revokes public/external sharing access for a file.
     */
    const revokePublicURL = Effect.fn("FilesService.revokePublicURL", {
      attributes: { "slack.method": "files.revokePublicURL" }
    })(
      (args: FilesRevokePublicURLArguments): Effect.Effect<FilesRevokePublicURLResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.files.revokePublicURL(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Enables a file for public/external sharing.
     */
    const sharedPublicURL = Effect.fn("FilesService.sharedPublicURL", {
      attributes: { "slack.method": "files.sharedPublicURL" }
    })(
      (args: FilesSharedPublicURLArguments): Effect.Effect<FilesSharedPublicURLResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.files.sharedPublicURL(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Uploads or creates a file.
     * @deprecated
     */
    const upload = Effect.fn("FilesService.upload", {
      attributes: { "slack.method": "files.upload" }
    })(
      (args: FilesUploadArguments): Effect.Effect<FilesUploadResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.files.upload(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Custom method to support a new way of uploading files to Slack. Supports a single file upload Supply: - (required) single file or content - (optional) channel, alt_text, snippet_type, Supports multiple file uploads Supply: - multiple upload_files Will try to honor both single file or content data supplied as well as multiple file uploads property.
     */
    const uploadV2 = Effect.fn("FilesService.uploadV2", {
      attributes: { "slack.method": "files.uploadV2" }
    })(
      (args: FilesUploadV2Arguments): Effect.Effect<WebAPICallResult, SlackError> =>
        Effect.tryPromise({
          try: () => client.files.uploadV2(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Deletes an existing comment on a file.
     */
    const CommentsDelete_ = Effect.fn("FilesService.comments.delete_", {
      attributes: { "slack.method": "files.comments.delete" }
    })(
      (args: FilesCommentsDeleteArguments): Effect.Effect<FilesCommentsDeleteResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.files.comments.delete(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Adds a file from a remote service.
     */
    const RemoteAdd = Effect.fn("FilesService.remote.add", {
      attributes: { "slack.method": "files.remote.add" }
    })(
      (args: FilesRemoteAddArguments): Effect.Effect<FilesRemoteAddResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.files.remote.add(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Retrieve information about a remote file added to Slack.
     */
    const RemoteInfo = Effect.fn("FilesService.remote.info", {
      attributes: { "slack.method": "files.remote.info" }
    })(
      (args: FilesRemoteInfoArguments): Effect.Effect<FilesRemoteInfoResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.files.remote.info(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * List remote files added to Slack.
     */
    const RemoteList = Effect.fn("FilesService.remote.list", {
      attributes: { "slack.method": "files.remote.list" }
    })(
      (args: FilesRemoteListArguments): Effect.Effect<FilesRemoteListResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.files.remote.list(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Remove a remote file.
     */
    const RemoteRemove = Effect.fn("FilesService.remote.remove", {
      attributes: { "slack.method": "files.remote.remove" }
    })(
      (args: FilesRemoteRemoveArguments): Effect.Effect<FilesRemoteRemoveResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.files.remote.remove(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Share a remote file into a channel.
     */
    const RemoteShare = Effect.fn("FilesService.remote.share", {
      attributes: { "slack.method": "files.remote.share" }
    })(
      (args: FilesRemoteShareArguments): Effect.Effect<FilesRemoteShareResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.files.remote.share(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Updates an existing remote file.
     */
    const RemoteUpdate = Effect.fn("FilesService.remote.update", {
      attributes: { "slack.method": "files.remote.update" }
    })(
      (args: FilesRemoteUpdateArguments): Effect.Effect<FilesRemoteUpdateResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.files.remote.update(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    return {
      completeUploadExternal,
      delete_,
      getUploadURLExternal,
      info,
      list,
      revokePublicURL,
      sharedPublicURL,
      upload,
      uploadV2,
      comments: {
        delete_: CommentsDelete_
      },
      remote: {
        add: RemoteAdd,
        info: RemoteInfo,
        list: RemoteList,
        remove: RemoteRemove,
        share: RemoteShare,
        update: RemoteUpdate
      }
    } as const
  })
}) {
  static readonly layer: Layer.Layer<FilesService, never, SlackClient> = Layer.effect(
    FilesService,
    FilesService.make
  )

  static readonly Default: Layer.Layer<FilesService, never, SlackConfig> = FilesService.layer.pipe(
    Layer.provide(SlackClient.Default)
  )
}
