/**
 * Generated Slack Reactions Service
 * DO NOT EDIT - This file is auto-generated by scripts/generate-services.ts
 */

import { Context, Effect, Layer } from "effect"
import type {
  ReactionsAddArguments,
  ReactionsAddResponse,
  ReactionsGetArguments,
  ReactionsGetResponse,
  ReactionsListArguments,
  ReactionsListResponse,
  ReactionsRemoveArguments,
  ReactionsRemoveResponse
} from "@slack/web-api"
import { SlackConfig } from "../SlackConfig.js"
import { SlackClient } from "../SlackClient.js"
import { mapSlackError, annotateSpanWithError, type SlackError } from "../internal/errors.js"

export class ReactionsService extends Context.Service<ReactionsService>()("effect-slack/ReactionsService", {
  make: Effect.gen(function* () {
    const client = yield* SlackClient

    /**
     * Adds a reaction to an item.
     */
    const add = Effect.fn("ReactionsService.add", {
      attributes: { "slack.method": "reactions.add" }
    })(
      (args: ReactionsAddArguments): Effect.Effect<ReactionsAddResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.reactions.add(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Gets reactions for an item.
     */
    const get = Effect.fn("ReactionsService.get", {
      attributes: { "slack.method": "reactions.get" }
    })(
      (args: ReactionsGetArguments): Effect.Effect<ReactionsGetResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.reactions.get(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * List reactions made by a user.
     */
    const list = Effect.fn("ReactionsService.list", {
      attributes: { "slack.method": "reactions.list" }
    })(
      (args?: ReactionsListArguments): Effect.Effect<ReactionsListResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.reactions.list(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Removes a reaction from an item.
     */
    const remove = Effect.fn("ReactionsService.remove", {
      attributes: { "slack.method": "reactions.remove" }
    })(
      (args: ReactionsRemoveArguments): Effect.Effect<ReactionsRemoveResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.reactions.remove(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    return {
      add,
      get,
      list,
      remove
    } as const
  })
}) {
  static readonly layer: Layer.Layer<ReactionsService, never, SlackClient> = Layer.effect(
    ReactionsService,
    ReactionsService.make
  )

  static readonly Default: Layer.Layer<ReactionsService, never, SlackConfig> = ReactionsService.layer.pipe(
    Layer.provide(SlackClient.Default)
  )
}
