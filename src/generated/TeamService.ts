/**
 * Generated Slack Team Service
 * DO NOT EDIT - This file is auto-generated by scripts/generate-services.ts
 */

import { Context, Effect, Layer } from "effect"
import type {
  TeamAccessLogsArguments,
  TeamAccessLogsResponse,
  TeamBillableInfoArguments,
  TeamBillableInfoResponse,
  TeamBillingInfoArguments,
  TeamBillingInfoResponse,
  TeamExternalTeamsDisconnectArguments,
  TeamExternalTeamsDisconnectResponse,
  TeamExternalTeamsListArguments,
  TeamExternalTeamsListResponse,
  TeamInfoArguments,
  TeamInfoResponse,
  TeamIntegrationLogsArguments,
  TeamIntegrationLogsResponse,
  TeamPreferencesListArguments,
  TeamPreferencesListResponse,
  TeamProfileGetArguments,
  TeamProfileGetResponse
} from "@slack/web-api"
import { SlackConfig } from "../SlackConfig.js"
import { SlackClient } from "../SlackClient.js"
import { mapSlackError, annotateSpanWithError, type SlackError } from "../internal/errors.js"

export class TeamService extends Context.Service<TeamService>()("effect-slack/TeamService", {
  make: Effect.gen(function* () {
    const client = yield* SlackClient

    /**
     * Gets the access logs for the current team.
     */
    const accessLogs = Effect.fn("TeamService.accessLogs", {
      attributes: { "slack.method": "team.accessLogs" }
    })(
      (args?: TeamAccessLogsArguments): Effect.Effect<TeamAccessLogsResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.team.accessLogs(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Gets billable users information for the current team.
     */
    const billableInfo = Effect.fn("TeamService.billableInfo", {
      attributes: { "slack.method": "team.billableInfo" }
    })(
      (args?: TeamBillableInfoArguments): Effect.Effect<TeamBillableInfoResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.team.billableInfo(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Gets information about the current team.
     */
    const info = Effect.fn("TeamService.info", {
      attributes: { "slack.method": "team.info" }
    })(
      (args?: TeamInfoArguments): Effect.Effect<TeamInfoResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.team.info(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Gets the integration logs for the current team.
     */
    const integrationLogs = Effect.fn("TeamService.integrationLogs", {
      attributes: { "slack.method": "team.integrationLogs" }
    })(
      (args?: TeamIntegrationLogsArguments): Effect.Effect<TeamIntegrationLogsResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.team.integrationLogs(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Reads a workspace's billing plan information.
     */
    const BillingInfo = Effect.fn("TeamService.billing.info", {
      attributes: { "slack.method": "team.billing.info" }
    })(
      (args: TeamBillingInfoArguments): Effect.Effect<TeamBillingInfoResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.team.billing.info(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Disconnect an external organization.
     */
    const ExternalTeamsDisconnect = Effect.fn("TeamService.externalTeams.disconnect", {
      attributes: { "slack.method": "team.externalTeams.disconnect" }
    })(
      (args: TeamExternalTeamsDisconnectArguments): Effect.Effect<TeamExternalTeamsDisconnectResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.team.externalTeams.disconnect(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Returns a list of all the external teams connected and details about the connection.
     */
    const ExternalTeamsList = Effect.fn("TeamService.externalTeams.list", {
      attributes: { "slack.method": "team.externalTeams.list" }
    })(
      (args: TeamExternalTeamsListArguments): Effect.Effect<TeamExternalTeamsListResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.team.externalTeams.list(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Retrieve a list of a workspace's team preferences.
     */
    const PreferencesList = Effect.fn("TeamService.preferences.list", {
      attributes: { "slack.method": "team.preferences.list" }
    })(
      (args?: TeamPreferencesListArguments): Effect.Effect<TeamPreferencesListResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.team.preferences.list(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Retrieve a team's profile.
     */
    const ProfileGet = Effect.fn("TeamService.profile.get", {
      attributes: { "slack.method": "team.profile.get" }
    })(
      (args?: TeamProfileGetArguments): Effect.Effect<TeamProfileGetResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.team.profile.get(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    return {
      accessLogs,
      billableInfo,
      info,
      integrationLogs,
      billing: {
        info: BillingInfo
      },
      externalTeams: {
        disconnect: ExternalTeamsDisconnect,
        list: ExternalTeamsList
      },
      preferences: {
        list: PreferencesList
      },
      profile: {
        get: ProfileGet
      }
    } as const
  })
}) {
  static readonly layer: Layer.Layer<TeamService, never, SlackClient> = Layer.effect(
    TeamService,
    TeamService.make
  )

  static readonly Default: Layer.Layer<TeamService, never, SlackConfig> = TeamService.layer.pipe(
    Layer.provide(SlackClient.Default)
  )
}
