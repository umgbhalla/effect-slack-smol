/**
 * Generated Slack Conversations Service
 * DO NOT EDIT - This file is auto-generated by scripts/generate-services.ts
 */

import { Context, Effect, Layer } from "effect"
import type {
  ConversationsAcceptSharedInviteArguments,
  ConversationsAcceptSharedInviteResponse,
  ConversationsApproveSharedInviteArguments,
  ConversationsApproveSharedInviteResponse,
  ConversationsArchiveArguments,
  ConversationsArchiveResponse,
  ConversationsCanvasesCreateArguments,
  ConversationsCanvasesCreateResponse,
  ConversationsCloseArguments,
  ConversationsCloseResponse,
  ConversationsCreateArguments,
  ConversationsCreateResponse,
  ConversationsDeclineSharedInviteArguments,
  ConversationsDeclineSharedInviteResponse,
  ConversationsExternalInvitePermissionsSetArguments,
  ConversationsExternalInvitePermissionsSetResponse,
  ConversationsHistoryArguments,
  ConversationsHistoryResponse,
  ConversationsInfoArguments,
  ConversationsInfoResponse,
  ConversationsInviteArguments,
  ConversationsInviteResponse,
  ConversationsInviteSharedArguments,
  ConversationsInviteSharedResponse,
  ConversationsJoinArguments,
  ConversationsJoinResponse,
  ConversationsKickArguments,
  ConversationsKickResponse,
  ConversationsLeaveArguments,
  ConversationsLeaveResponse,
  ConversationsListArguments,
  ConversationsListConnectInvitesArguments,
  ConversationsListConnectInvitesResponse,
  ConversationsListResponse,
  ConversationsMarkArguments,
  ConversationsMarkResponse,
  ConversationsMembersArguments,
  ConversationsMembersResponse,
  ConversationsOpenArguments,
  ConversationsOpenResponse,
  ConversationsRenameArguments,
  ConversationsRenameResponse,
  ConversationsRepliesArguments,
  ConversationsRepliesResponse,
  ConversationsRequestSharedInviteApproveArguments,
  ConversationsRequestSharedInviteApproveResponse,
  ConversationsRequestSharedInviteDenyArguments,
  ConversationsRequestSharedInviteDenyResponse,
  ConversationsRequestSharedInviteListArguments,
  ConversationsRequestSharedInviteListResponse,
  ConversationsSetPurposeArguments,
  ConversationsSetPurposeResponse,
  ConversationsSetTopicArguments,
  ConversationsSetTopicResponse,
  ConversationsUnarchiveArguments,
  ConversationsUnarchiveResponse
} from "@slack/web-api"
import { SlackConfig } from "../SlackConfig.js"
import { SlackClient } from "../SlackClient.js"
import { mapSlackError, annotateSpanWithError, type SlackError } from "../internal/errors.js"

export class ConversationsService extends Context.Service<ConversationsService>()("effect-slack/ConversationsService", {
  make: Effect.gen(function* () {
    const client = yield* SlackClient

    /**
     * Accepts an invitation to a Slack Connect channel.
     */
    const acceptSharedInvite = Effect.fn("ConversationsService.acceptSharedInvite", {
      attributes: { "slack.method": "conversations.acceptSharedInvite" }
    })(
      (args: ConversationsAcceptSharedInviteArguments): Effect.Effect<ConversationsAcceptSharedInviteResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.acceptSharedInvite(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Approves an invitation to a Slack Connect channel.
     */
    const approveSharedInvite = Effect.fn("ConversationsService.approveSharedInvite", {
      attributes: { "slack.method": "conversations.approveSharedInvite" }
    })(
      (args: ConversationsApproveSharedInviteArguments): Effect.Effect<ConversationsApproveSharedInviteResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.approveSharedInvite(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Archives a conversation.
     */
    const archive = Effect.fn("ConversationsService.archive", {
      attributes: { "slack.method": "conversations.archive" }
    })(
      (args: ConversationsArchiveArguments): Effect.Effect<ConversationsArchiveResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.archive(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Closes a direct message or multi-person direct message.
     */
    const close = Effect.fn("ConversationsService.close", {
      attributes: { "slack.method": "conversations.close" }
    })(
      (args: ConversationsCloseArguments): Effect.Effect<ConversationsCloseResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.close(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Initiates a public or private channel-based conversation.
     */
    const create = Effect.fn("ConversationsService.create", {
      attributes: { "slack.method": "conversations.create" }
    })(
      (args: ConversationsCreateArguments): Effect.Effect<ConversationsCreateResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.create(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Declines an invitation to a Slack Connect channel.
     */
    const declineSharedInvite = Effect.fn("ConversationsService.declineSharedInvite", {
      attributes: { "slack.method": "conversations.declineSharedInvite" }
    })(
      (args: ConversationsDeclineSharedInviteArguments): Effect.Effect<ConversationsDeclineSharedInviteResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.declineSharedInvite(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Fetches a conversation's history of messages and events.
     */
    const history = Effect.fn("ConversationsService.history", {
      attributes: { "slack.method": "conversations.history" }
    })(
      (args: ConversationsHistoryArguments): Effect.Effect<ConversationsHistoryResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.history(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Retrieve information about a conversation.
     */
    const info = Effect.fn("ConversationsService.info", {
      attributes: { "slack.method": "conversations.info" }
    })(
      (args: ConversationsInfoArguments): Effect.Effect<ConversationsInfoResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.info(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Invites users to a channel.
     */
    const invite = Effect.fn("ConversationsService.invite", {
      attributes: { "slack.method": "conversations.invite" }
    })(
      (args: ConversationsInviteArguments): Effect.Effect<ConversationsInviteResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.invite(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Sends an invitation to a Slack Connect channel.
     */
    const inviteShared = Effect.fn("ConversationsService.inviteShared", {
      attributes: { "slack.method": "conversations.inviteShared" }
    })(
      (args: ConversationsInviteSharedArguments): Effect.Effect<ConversationsInviteSharedResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.inviteShared(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Joins an existing conversation.
     */
    const join = Effect.fn("ConversationsService.join", {
      attributes: { "slack.method": "conversations.join" }
    })(
      (args: ConversationsJoinArguments): Effect.Effect<ConversationsJoinResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.join(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Removes a user from a conversation.
     */
    const kick = Effect.fn("ConversationsService.kick", {
      attributes: { "slack.method": "conversations.kick" }
    })(
      (args: ConversationsKickArguments): Effect.Effect<ConversationsKickResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.kick(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Leaves a conversation.
     */
    const leave = Effect.fn("ConversationsService.leave", {
      attributes: { "slack.method": "conversations.leave" }
    })(
      (args: ConversationsLeaveArguments): Effect.Effect<ConversationsLeaveResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.leave(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * List all channels in a Slack team.
     */
    const list = Effect.fn("ConversationsService.list", {
      attributes: { "slack.method": "conversations.list" }
    })(
      (args?: ConversationsListArguments): Effect.Effect<ConversationsListResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.list(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Lists shared channel invites that have been generated or received but have not been approved by all parties.
     */
    const listConnectInvites = Effect.fn("ConversationsService.listConnectInvites", {
      attributes: { "slack.method": "conversations.listConnectInvites" }
    })(
      (args?: ConversationsListConnectInvitesArguments): Effect.Effect<ConversationsListConnectInvitesResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.listConnectInvites(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Sets the read cursor in a channel.
     */
    const mark = Effect.fn("ConversationsService.mark", {
      attributes: { "slack.method": "conversations.mark" }
    })(
      (args: ConversationsMarkArguments): Effect.Effect<ConversationsMarkResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.mark(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Retrieve members of a conversation.
     */
    const members = Effect.fn("ConversationsService.members", {
      attributes: { "slack.method": "conversations.members" }
    })(
      (args: ConversationsMembersArguments): Effect.Effect<ConversationsMembersResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.members(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Opens or resumes a direct message or multi-person direct message.
     */
    const open = Effect.fn("ConversationsService.open", {
      attributes: { "slack.method": "conversations.open" }
    })(
      (args: ConversationsOpenArguments): Effect.Effect<ConversationsOpenResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.open(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Renames a conversation.
     */
    const rename = Effect.fn("ConversationsService.rename", {
      attributes: { "slack.method": "conversations.rename" }
    })(
      (args: ConversationsRenameArguments): Effect.Effect<ConversationsRenameResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.rename(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Retrieve a thread of messages posted to a conversation.
     */
    const replies = Effect.fn("ConversationsService.replies", {
      attributes: { "slack.method": "conversations.replies" }
    })(
      (args: ConversationsRepliesArguments): Effect.Effect<ConversationsRepliesResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.replies(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Sets the purpose for a conversation.
     */
    const setPurpose = Effect.fn("ConversationsService.setPurpose", {
      attributes: { "slack.method": "conversations.setPurpose" }
    })(
      (args: ConversationsSetPurposeArguments): Effect.Effect<ConversationsSetPurposeResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.setPurpose(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Sets the topic for a conversation.
     */
    const setTopic = Effect.fn("ConversationsService.setTopic", {
      attributes: { "slack.method": "conversations.setTopic" }
    })(
      (args: ConversationsSetTopicArguments): Effect.Effect<ConversationsSetTopicResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.setTopic(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Reverses conversation archival.
     */
    const unarchive = Effect.fn("ConversationsService.unarchive", {
      attributes: { "slack.method": "conversations.unarchive" }
    })(
      (args: ConversationsUnarchiveArguments): Effect.Effect<ConversationsUnarchiveResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.unarchive(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Create a Channel Canvas for a channel.
     */
    const CanvasesCreate = Effect.fn("ConversationsService.canvases.create", {
      attributes: { "slack.method": "conversations.canvases.create" }
    })(
      (args: ConversationsCanvasesCreateArguments): Effect.Effect<ConversationsCanvasesCreateResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.canvases.create(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Convert a team in a shared channel from an External Limited channel to a fully shared Slack Connect channel or vice versa.
     */
    const ExternalInvitePermissionsSet = Effect.fn("ConversationsService.externalInvitePermissions.set", {
      attributes: { "slack.method": "conversations.externalInvitePermissions.set" }
    })(
      (args: ConversationsExternalInvitePermissionsSetArguments): Effect.Effect<ConversationsExternalInvitePermissionsSetResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.externalInvitePermissions.set(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Approves a request to add an external user to a channel and sends them a Slack Connect invite.
     */
    const RequestSharedInviteApprove = Effect.fn("ConversationsService.requestSharedInvite.approve", {
      attributes: { "slack.method": "conversations.requestSharedInvite.approve" }
    })(
      (args: ConversationsRequestSharedInviteApproveArguments): Effect.Effect<ConversationsRequestSharedInviteApproveResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.requestSharedInvite.approve(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Denies a request to invite an external user to a channel.
     */
    const RequestSharedInviteDeny = Effect.fn("ConversationsService.requestSharedInvite.deny", {
      attributes: { "slack.method": "conversations.requestSharedInvite.deny" }
    })(
      (args: ConversationsRequestSharedInviteDenyArguments): Effect.Effect<ConversationsRequestSharedInviteDenyResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.requestSharedInvite.deny(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Lists requests to add external users to channels with ability to filter.
     */
    const RequestSharedInviteList = Effect.fn("ConversationsService.requestSharedInvite.list", {
      attributes: { "slack.method": "conversations.requestSharedInvite.list" }
    })(
      (args?: ConversationsRequestSharedInviteListArguments): Effect.Effect<ConversationsRequestSharedInviteListResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.conversations.requestSharedInvite.list(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    return {
      acceptSharedInvite,
      approveSharedInvite,
      archive,
      close,
      create,
      declineSharedInvite,
      history,
      info,
      invite,
      inviteShared,
      join,
      kick,
      leave,
      list,
      listConnectInvites,
      mark,
      members,
      open,
      rename,
      replies,
      setPurpose,
      setTopic,
      unarchive,
      canvases: {
        create: CanvasesCreate
      },
      externalInvitePermissions: {
        set: ExternalInvitePermissionsSet
      },
      requestSharedInvite: {
        approve: RequestSharedInviteApprove,
        deny: RequestSharedInviteDeny,
        list: RequestSharedInviteList
      }
    } as const
  })
}) {
  static readonly layer: Layer.Layer<ConversationsService, never, SlackClient> = Layer.effect(
    ConversationsService,
    ConversationsService.make
  )

  static readonly Default: Layer.Layer<ConversationsService, never, SlackConfig> = ConversationsService.layer.pipe(
    Layer.provide(SlackClient.Default)
  )
}
