/**
 * Generated Slack Dnd Service
 * DO NOT EDIT - This file is auto-generated by scripts/generate-services.ts
 */

import { Context, Effect, Layer } from "effect"
import type {
  DndEndDndArguments,
  DndEndDndResponse,
  DndEndSnoozeArguments,
  DndEndSnoozeResponse,
  DndInfoArguments,
  DndInfoResponse,
  DndSetSnoozeArguments,
  DndSetSnoozeResponse,
  DndTeamInfoArguments,
  DndTeamInfoResponse
} from "@slack/web-api"
import { SlackConfig } from "../SlackConfig.js"
import { SlackClient } from "../SlackClient.js"
import { mapSlackError, annotateSpanWithError, type SlackError } from "../internal/errors.js"

export class DndService extends Context.Service<DndService>()("effect-slack/DndService", {
  make: Effect.gen(function* () {
    const client = yield* SlackClient

    /**
     * Ends the current user's Do Not Disturb session immediately.
     */
    const endDnd = Effect.fn("DndService.endDnd", {
      attributes: { "slack.method": "dnd.endDnd" }
    })(
      (args?: DndEndDndArguments): Effect.Effect<DndEndDndResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.dnd.endDnd(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Ends the current user's snooze mode immediately.
     */
    const endSnooze = Effect.fn("DndService.endSnooze", {
      attributes: { "slack.method": "dnd.endSnooze" }
    })(
      (args?: DndEndSnoozeArguments): Effect.Effect<DndEndSnoozeResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.dnd.endSnooze(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Retrieves a user's current Do Not Disturb status.
     */
    const info = Effect.fn("DndService.info", {
      attributes: { "slack.method": "dnd.info" }
    })(
      (args?: DndInfoArguments): Effect.Effect<DndInfoResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.dnd.info(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Turns on Do Not Disturb mode for the current user, or changes its duration.
     */
    const setSnooze = Effect.fn("DndService.setSnooze", {
      attributes: { "slack.method": "dnd.setSnooze" }
    })(
      (args: DndSetSnoozeArguments): Effect.Effect<DndSetSnoozeResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.dnd.setSnooze(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Retrieves the Do Not Disturb status for up to 50 users on a team.
     */
    const teamInfo = Effect.fn("DndService.teamInfo", {
      attributes: { "slack.method": "dnd.teamInfo" }
    })(
      (args: DndTeamInfoArguments): Effect.Effect<DndTeamInfoResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.dnd.teamInfo(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    return {
      endDnd,
      endSnooze,
      info,
      setSnooze,
      teamInfo
    } as const
  })
}) {
  static readonly layer: Layer.Layer<DndService, never, SlackClient> = Layer.effect(
    DndService,
    DndService.make
  )

  static readonly Default: Layer.Layer<DndService, never, SlackConfig> = DndService.layer.pipe(
    Layer.provide(SlackClient.Default)
  )
}
