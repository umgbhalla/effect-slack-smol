/**
 * Generated Slack Views Service
 * DO NOT EDIT - This file is auto-generated by scripts/generate-services.ts
 */

import { Context, Effect, Layer } from "effect"
import type {
  ViewsOpenArguments,
  ViewsOpenResponse,
  ViewsPublishArguments,
  ViewsPublishResponse,
  ViewsPushArguments,
  ViewsPushResponse,
  ViewsUpdateArguments,
  ViewsUpdateResponse
} from "@slack/web-api"
import { SlackConfig } from "../SlackConfig.js"
import { SlackClient } from "../SlackClient.js"
import { mapSlackError, annotateSpanWithError, type SlackError } from "../internal/errors.js"

export class ViewsService extends Context.Service<ViewsService>()("effect-slack/ViewsService", {
  make: Effect.gen(function* () {
    const client = yield* SlackClient

    /**
     * Open a view for a user.
     */
    const open = Effect.fn("ViewsService.open", {
      attributes: { "slack.method": "views.open" }
    })(
      (args: ViewsOpenArguments): Effect.Effect<ViewsOpenResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.views.open(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Publish a static view for a user.
     */
    const publish = Effect.fn("ViewsService.publish", {
      attributes: { "slack.method": "views.publish" }
    })(
      (args: ViewsPublishArguments): Effect.Effect<ViewsPublishResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.views.publish(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Push a view onto the stack of a root view.
     */
    const push = Effect.fn("ViewsService.push", {
      attributes: { "slack.method": "views.push" }
    })(
      (args: ViewsPushArguments): Effect.Effect<ViewsPushResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.views.push(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    /**
     * Update an existing view.
     */
    const update = Effect.fn("ViewsService.update", {
      attributes: { "slack.method": "views.update" }
    })(
      (args: ViewsUpdateArguments): Effect.Effect<ViewsUpdateResponse, SlackError> =>
        Effect.tryPromise({
          try: () => client.views.update(args),
          catch: mapSlackError
        }).pipe(Effect.tapError(annotateSpanWithError))
    )

    return {
      open,
      publish,
      push,
      update
    } as const
  })
}) {
  static readonly layer: Layer.Layer<ViewsService, never, SlackClient> = Layer.effect(
    ViewsService,
    ViewsService.make
  )

  static readonly Default: Layer.Layer<ViewsService, never, SlackConfig> = ViewsService.layer.pipe(
    Layer.provide(SlackClient.Default)
  )
}
